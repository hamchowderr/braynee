#!/usr/bin/env bash
# Thin wrapper around QMD (Quick Memory Database) search engine.
# Usage: qmd search "query"  |  qmd vsearch "query"  |  qmd index  |  qmd status
exec node "$HOME/.claude/scripts/qmd-wrapper.mjs" "$@"
