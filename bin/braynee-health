#!/usr/bin/env bash
# Run the braynee health check (setup, connections, memory, inbox).
# Usage: braynee-health [check|setup|connections|memory|inbox] [--vault /path]
PLUGIN_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec python3 "$PLUGIN_DIR/skills/health/scripts/health.py" "$@"
