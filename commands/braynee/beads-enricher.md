---
description: Upgrade freshly-seeded beads stubs into agent-grade issues and wire build-order dependencies — the missing step between `prd-seed` and `braynee:autopilot`.
---

<!-- GENERATED by scripts/gen-mastracode-commands.mjs — do not edit by hand. -->

Delegate this to braynee's **beads-enricher** agent.

Call the `subagent` tool with:

- `agentType`: `"explore"` — this agent is read-only and must not modify the workspace.
- `task`: a self-contained brief. A non-forked subagent cannot see this
  conversation, so restate everything it needs rather than referring back.

The brief must tell the subagent to first read braynee's playbook for this role
at `agents/beads-enricher.md` inside the braynee plugin root — the braynee plugin
instructions give its absolute path — and then follow that playbook exactly. The
playbook is the source of truth for how this agent works; do not restate it here.

Request: $ARGUMENTS
